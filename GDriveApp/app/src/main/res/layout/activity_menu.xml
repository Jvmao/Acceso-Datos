<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeMenu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FE2E2E"
    tools:context="com.josevicente.gdriveapp.MenuActivity">

    <Button
        android:id="@+id/btnCrear"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:background="@drawable/rounded_button"
        android:text="@string/btnCrear"
        android:textColor="@android:color/background_light"
        android:textSize="18sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnListar"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/btnCrear"
        android:layout_below="@+id/btnCrear"
        android:layout_marginTop="50dp"
        android:background="@drawable/rounded_button"
        android:text="@string/btnListar"
        android:textColor="?android:attr/colorFocusedHighlight"
        android:textSize="18sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnBuscar"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/btnListar"
        android:layout_below="@+id/btnListar"
        android:layout_marginTop="50dp"
        android:background="@drawable/rounded_button"
        android:text="@string/btnBuscar"
        android:textColor="?attr/actionModeSplitBackground"
        android:textSize="18sp"
        android:textStyle="bold" />
</RelativeLayout>
